version: '3.8'

services:
  user-service:
    build: ./services/user-service
    ports:
      - "8000:8000" 
    env_file:
      - ./services/user-service/.env
    networks:
      - ecommerce-network

networks:
  ecommerce-network:
    driver: bridge
